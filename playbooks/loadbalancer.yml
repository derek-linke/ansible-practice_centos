---
- name: LoadBalancer Setup
  hosts: loadbalancer
  become: true

  tasks:
    - name: Install nginx
      apt: 
        name: nginx
        state: present
        update_cache: yes

    - name: Ensure nginx Started
      service:
        name: nginx
        state: started
        enabled: yes
